# angular2-todo
Todo App written in AngularJS 2.0 Beta & TypeScript following steps from egghead.io course.

## to run the app
- clone the repo
- npm install ( this will also install typings required )
- npm start
